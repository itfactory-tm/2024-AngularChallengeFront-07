export interface Ticket {
    ticketId: string;
    editionId: string;
    editionName: string;
    ticketTypeId: string;
    ticketPrice: number;
    dayId: string;
    dayName: string;
    amountTickets: number;
}
